# Database (REQUERIDO)
# Obtén este string de tu proveedor de base de datos (Neon, Supabase, Railway, etc.)
DATABASE_URL="postgresql://user:password@localhost:5432/trading_platform?schema=public"

# App URL - OPCIONAL
# Solo configura estas variables si tienes un dominio custom
# Si NO las configuras, Vercel usará automáticamente VERCEL_URL (que cambia en cada deployment)

# Para dominio custom o producción:
# NEXT_PUBLIC_APP_URL="https://tu-dominio-custom.com"
# BETTER_AUTH_URL="https://tu-dominio-custom.com"

# Para desarrollo local:
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Node Environment
NODE_ENV="development"

# NOTA IMPORTANTE PARA VERCEL:
# NO necesitas configurar VERCEL_URL manualmente - Vercel lo hace automáticamente
# El código ya está configurado para usar VERCEL_URL automáticamente en cada deployment

